# Maps the Upgrade request header to the right Connection header, so the same
# location can serve plain HTTP and WebSocket upgrades (used by /ws).

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
